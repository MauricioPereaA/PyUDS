
            # This is and autogenerated test case using PyUDS Test Builder v0.1 #
        
from Testcases.TestClass import TestCase
from inspect import stack as info
import unittest, time

test = TestCase()
class PyUDS_TestCase(unittest.TestCase):

    #== Positive Flow Diagnostic Session Control Session and Security Tests ==#
    @classmethod
    def setUpClass(self):
        #== Initialize test case ==#
        test.begin(
            test_info=info(),
            writeTestResults=False,
            step_delay=0.005
        )

    @classmethod
    def tearDownClass(self):
        #== End Test Case ==#
        test.end()


    def test_001(self, name='Clear DTCs after 5 seconds'):
        #test.read_power_supply_voltage()

        test.set_dtc_condition(underVoltage=True)
        input('Confirm your Power supply is set to under voltage')

        test.power_supply_reset_default()
        input('Confirm your Power supply is set to normal voltage')

        test.set_dtc_condition(overVoltage=True)
        input('Confirm your Power supply is set to over voltage')

        test.power_supply_reset_default()
        input('Confirm your Power supply is set to normal voltage')