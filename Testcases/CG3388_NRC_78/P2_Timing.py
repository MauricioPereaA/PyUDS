           # This is and autogenerated test case using PyUDS Test Builder v0.2 #
from framework.shared_functions import device_under_test
from Testcases.TestClass import TestCase
from inspect import stack as info
import time
import unittest

test = TestCase()

class PyUDS_TestCase(unittest.TestCase):

    #== PyUDS - Autogenerated Test Case Template ==#
    @classmethod
    def setUpClass(self):
        #== Initialize test case ==#
        test.begin(
            test_info=info(),
            writeTestResults=True,
            excel_tab='NRC 78 & P2 Timing'
        )
        self.P2sever =[0,0x64]
        self.P2_sever =[0,0x1388]
        
    @classmethod
    def tearDownClass(self):
        #== End Test Case ==#
        test.end()
        
    def test_002(self, name='transition to defaultSession'):
        test.preconditions(
            step_info=info()
        )
        test.step(
            step_title=name,
            custom='10 01',
            expected={
                'response': 'Positive',
                'P2sever':  self.P2sever,
                'P2*sever':  self.P2_sever
            }
        )

    def test_004(self,name='ransition to extendedSession'):
        test.preconditions(
             step_info=info()          
        )
        test.step(
            step_title=name,
            custom='10 03',
            expected={
                'response': 'Positive',
                'P2sever':  self.P2sever,
                'P2*sever':  self.P2_sever
            }
        )
   

#     def test_006(self, name='Send physical Logical Address Request'):
#         test.preconditions(
#             step_info=info()
#         )
#         test.step(
#             step_title=name,
#             custom='0E F4 0C A8 00 00 00 02 10 02',
#             expected={
#                 'response': 'Positive',
#                 'P2sever':  self.P2sever,
#                 'P2*sever':  self.P2_sever
#             }
#         )
            